'use client'
export default function ExportsPanel({storeId}:{storeId:string}){const items=[['products','Produtos','Produtos, preços, custos e estoque atual.'],['inventory','Estoque','Quantidades e estoque mínimo.'],['sales','Vendas','Receita, CMV e lucro bruto por venda.'],['transactions','Financeiro','Movimentações bancárias read-only.']] as const;return <section className="rounded-2xl border p-5"><h3 className="text-lg font-bold">Usar meus dados fora do Balcão</h3><p className="mt-1 text-sm text-slate-500">Baixe CSV compatível com Excel ou use a API avançada para Power BI e outros sistemas.</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{items.map(([kind,title,desc])=><a key={kind} href={`/api/balcao/automations/exports/${kind}?storeId=${encodeURIComponent(storeId)}`} className="rounded-xl border p-4 hover:bg-slate-50"><b>{title}</b><span className="mt-1 block text-sm text-slate-500">{desc}</span><span className="mt-3 block text-sm font-bold text-blue-700">Baixar CSV →</span></a>)}</div></section>}
