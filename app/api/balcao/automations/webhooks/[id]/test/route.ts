import { createAdminClient } from '@/lib/supabase/admin'
import { requireAutomationAccess, automationAccessResponse } from '@/lib/platform/auth/automationsContext'
import { decryptWebhookSecret } from '@/lib/platform/webhooks/crypto'
import { signWebhookBody } from '@/lib/platform/webhooks/signing'
export async function POST(_request:Request,{params}:{params:Promise<{id:string}>}){ try{ const {id}=await params; const admin=createAdminClient(); const {data:row}=await admin.from('balcao_webhook_endpoints').select('business_id,store_id,url,secret_ciphertext').eq('id',id).maybeSingle(); if(!row?.store_id) return Response.json({error:'Webhook não encontrado.'},{status:404}); const actor=await requireAutomationAccess(String(row.store_id),'webhooks.manage'); if(String(row.business_id)!==actor.businessId) return Response.json({error:'Webhook não encontrado.'},{status:404}); const eventId=crypto.randomUUID(); const timestamp=Math.floor(Date.now()/1000).toString(); const payload={id:eventId,type:'webhook.test',apiVersion:'2026-09-12',occurredAt:new Date().toISOString(),businessId:actor.businessId,storeId:actor.storeId,data:{message:'Teste do BALCÃO'}}; const body=JSON.stringify(payload); const secret=decryptWebhookSecret(String(row.secret_ciphertext)); const response=await fetch(String(row.url),{method:'POST',headers:{'content-type':'application/json','X-RPG-Event-Id':eventId,'X-RPG-Timestamp':timestamp,'X-RPG-Signature':signWebhookBody(secret,timestamp,body)},body,signal:AbortSignal.timeout(10_000)}); return Response.json({ok:response.ok,status:response.status},{status:response.ok?200:502}) }catch(error){ return automationAccessResponse(error) } }
