import { createAdminClient } from '@/lib/supabase/admin'
import { requireAutomationAccess, automationAccessResponse } from '@/lib/platform/auth/automationsContext'
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){ try{ const {id}=await params; const admin=createAdminClient(); const {data:row}=await admin.from('balcao_webhook_endpoints').select('business_id,store_id').eq('id',id).maybeSingle(); if(!row?.store_id) return Response.json({error:'Webhook não encontrado.'},{status:404}); const actor=await requireAutomationAccess(String(row.store_id),'webhooks.manage'); if(String(row.business_id)!==actor.businessId) return Response.json({error:'Webhook não encontrado.'},{status:404}); const {data,error}=await admin.from('balcao_webhook_deliveries').select('id,event_id,status,attempt_count,response_status,duration_ms,last_error,delivered_at,created_at,balcao_event_outbox(event_type,occurred_at)').eq('endpoint_id',id).order('created_at',{ascending:false}).limit(50); if(error) throw error; return Response.json({deliveries:data??[]}) }catch(error){ return automationAccessResponse(error) } }
