'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
export default function ActivateDevice({ token }: { token: string }) { const router = useRouter(); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); async function activate(){setBusy(true);setError('');const r=await fetch('/api/balcao/terminals/activate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token})});const p=await r.json().catch(()=>({}));if(!r.ok){setError(p.error||'Não conseguimos ativar.');setBusy(false);return}router.replace('/work');router.refresh()} return <section className="mx-auto max-w-md rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold tracking-[.16em] text-blue-700">BALCÃO</p><h1 className="mt-4 text-2xl font-bold">Ativar este dispositivo</h1><p className="mt-3 leading-7 text-slate-600">Depois da ativação, os funcionários desta loja poderão entrar aqui usando apenas o PIN.</p>{error?<p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-sm font-semibold text-red-800">{error}</p>:null}<button onClick={activate} disabled={busy} className="mt-6 min-h-12 w-full rounded-xl bg-blue-700 px-5 py-3 text-base font-semibold text-white disabled:opacity-60">{busy?'Ativando…':'Ativar dispositivo'}</button></section> }
